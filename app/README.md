Конечно! Вот пример файла **`README.md`** для твоего проекта с Streamlit-приложением, включая структуру проекта, зависимости, полезные команды и инструкции по запуску и тестированию.

---

# 🍽️ Поиск заведений в Москве

Простое Streamlit-приложение для поиска кафе, ресторанов и других заведений в Москве по типу, району и количеству мест. Результаты автоматически сортируются по рейтингу (от высокого к низкому).

---

## 📁 Структура проекта

```
.
├── app.py                     # Основное Streamlit-приложение
├── test_app.py                # Автоматические тесты
├── data/
│   └── moscow_places.csv      # Данные о заведениях (должен существовать!)
└── README.md
```

---

## 🧰 Требования

- Python 3.8+
- Библиотеки: `streamlit`, `pandas`

Установи зависимости:

```bash
pip install -r requirements.txt
```

Если файла `requirements.txt` нет — создай его:

```txt
# requirements.txt
streamlit>=1.30
pandas>=1.5
```

Или установи вручную:

```bash
pip install streamlit pandas
```

---

## ▶️ Запуск приложения

Убедись, что файл данных существует:

```
data/moscow_places.csv
```

Затем запусти приложение:

```bash
streamlit run app.py
```

Приложение откроется в браузере по адресу: [http://localhost:8501](http://localhost:8501)

---

## 🧪 Тестирование

Запуск юнит-тестов (проверяют фильтрацию и сортировку):

```bash
python -m unittest test_app.py -v
```

Или через `pytest` (если установлен):

```bash
pytest test_app.py -v
```

> 💡 Тесты **не требуют** наличия `moscow_places.csv` — они используют встроенные фикстуры.

---

## 📥 Формат данных (`moscow_places.csv`)

Файл должен быть в кодировке **UTF-8** и содержать следующие колонки:

| Колонка       | Тип       | Обязательная | Пример         |
|---------------|-----------|--------------|----------------|
| `name`        | строка    | ❌            | Кофемания      |
| `category`    | строка    | ✅            | Кафе           |
| `district`    | строка    | ✅            | Центр          |
| `seats`       | число     | ✅            | 40             |
| `rating`      | число (0–5)| ✅           | 4.5            |

Пример CSV:

```csv
name,category,district,seats,rating
Кофемания,Кафе,Центр,30,4.2
Гастрономика,Ресторан,Арбат,80,4.8
```

> ⚠️ Если у заведения нет рейтинга — оставь ячейку пустой или поставь `NaN`.

---

## 🛠️ Полезные команды

| Действие                          | Команда                                      |
|----------------------------------|----------------------------------------------|
| Запуск приложения                | `streamlit run app.py`                       |
| Проверка версии Streamlit        | `streamlit --version`                        |
| Запуск тестов                    | `python -m unittest test_app.py -v`          |
| Создание виртуального окружения  | `python -m venv venv`                        |
| Активация venv (macOS/Linux)     | `source venv/bin/activate`                   |
| Активация venv (Windows)         | `venv\Scripts\activate`                      |
| Сохранение зависимостей          | `pip freeze > requirements.txt`              |
| Обновление зависимостей          | `pip install -r requirements.txt --upgrade`  |

---

## 📬 Поддержка

Если данные не загружаются:
- Проверь путь к `CSV_FILE` в `app.py`
- Убедись, что файл существует и доступен для чтения
- Проверь наличие всех обязательных колонок

---

## 📄 Лицензия

MIT — используй свободно для учебы и работы.


